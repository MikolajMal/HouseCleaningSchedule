<UserControl x:Class="HouseCleaningSchedule.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HouseCleaningSchedule.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400">
    <UserControl.Resources>
        <DataTemplate x:Key="RoomDataTemplate">
            <Button Command="{Binding DataContext.ShowRoomCommand, 
                              RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock FontSize="18" Foreground="{StaticResource TextBrush}" Text="{Binding Name}" Margin="20,2"/>
                    <TextBlock Grid.Column="1" Grid.RowSpan="2" Foreground="{StaticResource SecondaryTextBrush}" Text="Percentage" TextAlignment="Right" VerticalAlignment="Center" Margin="20,5"/>
                </Grid>
            </Button>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <StackPanel Background="{StaticResource MainBackgroundBrush}">
            <ItemsControl ItemsSource="{Binding Rooms}" ItemTemplate="{StaticResource RoomDataTemplate}"/>
        </StackPanel>
    </Grid>
</UserControl>
